--- arj-3.10.22/makefile	2004-06-18 18:19:36.000000000 +0200
+++ arj/makefile	2025-12-26 16:54:16.269018610 +0100
@@ -101,7 +101,7 @@
 CC = bcc.exe
 LINKER = tlink.exe
 COPT = -I$(BASEDIR) -o$@ @settings\$(CC_CODE).sts %s
-LINKLIB =
+LINKLIB = C:\cpp31\lib\
 LINKOPT =
 !else if "$(COMPILER)" == "BC40"
 CC_CODE = $(COMPILER)
@@ -950,6 +950,7 @@
 $(MAP_LINKOPT)$(@R).map
 $(STD_LIB)
 <<
+ type $(BASEDIR_T)today.lrf
  $(LINKER) @$(BASEDIR_T)today.lrf$(LAST_LINKOPT)
  if exist $(BASEDIR_T)today.lrf del $(BASEDIR_T)today.lrf
 
